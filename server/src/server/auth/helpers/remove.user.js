// this code is to remove user no confirmed


const removeUserNoConfirmed = (id, email) => {
    setTimeout(async () => {
        console.log('log in 5 secons'); // TODO: you need to end this function
    }, 1000*10);
}
removeUserNoConfirmed()

export default removeUserNoConfirmed;